use tokio::sync::Mutex;

pub struct GreetingControllerImpl {
    pub guard: Mutex<()>,
}

use std::{fs, net};

use std::netlink::Route;

#[cfg(test)]
mod tests {
    use tokio::runtime::Runtime;

    #[tokio::test]
    async fn a_greeting_is_returned() {}
}

use std::{
    process,
};
