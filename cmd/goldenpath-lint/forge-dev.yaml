name: goldenpath-lint
kind: mcp-server
profile: test-runner
version: 0.1.0
description: Fails a repo when its layout departs from the hexagonal golden path for its language.
openapi:
  specPath: ./spec.openapi.yaml
generate:
  packageName: main
